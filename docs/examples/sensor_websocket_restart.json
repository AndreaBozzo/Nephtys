{
  "id": "gateway_bounded_restart",
  "kind": "websocket",
  "url": "wss://gateway.example.com/telemetry",
  "topic": "nephtys.stream.sensors.gateway",
  "websocket": {
    "on_connect_send": "{\"action\":\"subscribe\",\"channel\":\"sensors\"}"
  },
  "restart": {
    "max_attempts": 10,
    "initial_backoff": "1s",
    "max_backoff": "30s",
    "factor": 2.0,
    "reset_after": "60s"
  },
  "pipeline": {
    "enrich": {
      "tags": {
        "site": "plant-a",
        "profile": "sensor-gateway"
      }
    }
  }
}
